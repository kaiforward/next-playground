/** See `app/(game)/@panel/system/layout.tsx`'s docstring — same passthrough, same reason. */
export default function FactionPanelLayout({ children }: { children: React.ReactNode }) {
  return children;
}
