---
name: spec-review
description: Adversarial multi-agent review of a feature spec against the existing codebase. Use when the user runs /spec-review with a document path, or when an approved spec with cross-mechanic surface is about to go to implementation planning.
---

Read `${CLAUDE_PROJECT_DIR}/.agents/skills/spec-review/SKILL.md` completely and follow it as the authoritative workflow. Resolve its relative references from `${CLAUDE_PROJECT_DIR}/.agents/skills/spec-review/` and capability tiers through `${CLAUDE_PROJECT_DIR}/.agents/model-tiers.md`.
