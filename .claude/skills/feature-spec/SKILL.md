---
name: feature-spec
description: Write the functional spec for a feature from its measured evidence — observable behaviour, the hazard worksheet filled with receipts, and what the spec does not claim. Use when the user runs /feature-spec, or when a feature's evidence exists and design is the next step. Refuses to start without evidence.
---

Read `${CLAUDE_PROJECT_DIR}/.agents/skills/feature-spec/SKILL.md` completely and follow it as the authoritative workflow. Resolve its relative references from `${CLAUDE_PROJECT_DIR}/.agents/skills/feature-spec/`, and the design hazards it references from `${CLAUDE_PROJECT_DIR}/.agents/skills/shared/design-hazards.md`.
