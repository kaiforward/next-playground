---
name: implement-plan
description: Execute a committed build plan task-by-task with dispatched implementers. Use when the user runs /implement-plan, or when a build plan exists in docs/build-plans/ and implementation is about to start.
---

Read `${CLAUDE_PROJECT_DIR}/.agents/skills/implement-plan/SKILL.md` completely and follow it as the authoritative workflow. Resolve its relative references from `${CLAUDE_PROJECT_DIR}/.agents/skills/implement-plan/` and capability tiers through `${CLAUDE_PROJECT_DIR}/.agents/model-tiers.md`.
