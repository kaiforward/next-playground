---
name: brainstorm
description: Shape a feature or mechanic idea into one chosen direction with stated premises and a committed falsifier, ready for /measure. Use when the user runs /brainstorm, or when a roadmap item or new idea is about to be designed and no evidence or spec exists yet.
---

Read `${CLAUDE_PROJECT_DIR}/.agents/skills/brainstorm/SKILL.md` completely and follow it as the authoritative workflow. Resolve its relative references from `${CLAUDE_PROJECT_DIR}/.agents/skills/brainstorm/`, and the design hazards it references from `${CLAUDE_PROJECT_DIR}/.agents/skills/shared/design-hazards.md`.
